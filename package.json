{
  "name": "hackathon-control-bitecode",
  "version": "2.0.0",
  "private": true,
  "description": "BiteCode Hackathon Control platform",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "node start.js",
    "dev": "node start.js",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "setup": "npm install --prefix backend && npm install --prefix frontend && npm run seed --prefix backend"
  }
}
